// Generated by CoffeeScript 1.8.0
(function() {
  app.templates.path = function(doc, type, entry) {
    var html;
    html = "<a href=\"" + (doc.fullPath()) + "\" class=\"_path-item _icon-" + doc.slug + "\">" + doc.name + "</a>";
    if (type) {
      html += "<a href=\"" + (type.fullPath()) + "\" class=\"_path-item\">" + type.name + "</a>";
    }
    if (entry) {
      html += "<span class=\"_path-item\">" + ($.escape(entry.name)) + "</span>";
    }
    return html;
  };

}).call(this);
